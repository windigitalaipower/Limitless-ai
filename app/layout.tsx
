export const metadata = {
  title: 'Limitless AI',
  description: 'Premium AI-Powered Content Refiner',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body style={{ margin: 0, padding: 0, backgroundColor: 'black' }}>
        {children}
      </body>
    </html>
  )
}
